% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rindep_norm_gamma_reg_v2.R
\name{rindependent_norm_gamma_reg_v2}
\alias{rindependent_norm_gamma_reg_v2}
\title{The Bayesian Indendepent Normal-Gamma Regression Distribution}
\usage{
rindependent_norm_gamma_reg_v2(
  n,
  y,
  x,
  prior_list,
  offset = NULL,
  weights = 1,
  family = gaussian()
)
}
\arguments{
\item{n}{number of draws to generate. If \code{length(n) > 1}, the length is taken to be the number required.}

\item{y}{For \code{glm}:
    logical values indicating whether the response vector and model
    matrix used in the fitting process should be returned as components
    of the returned value.

    For \code{glm.fit}: \code{x} is a design matrix of dimension
    \code{n * p}, and \code{y} is a vector of observations of length
    \code{n}.
  }

\item{x}{For \code{glm}:
    logical values indicating whether the response vector and model
    matrix used in the fitting process should be returned as components
    of the returned value.

    For \code{glm.fit}: \code{x} is a design matrix of dimension
    \code{n * p}, and \code{y} is a vector of observations of length
    \code{n}.
  }

\item{prior_list}{a list with the prior parameters (mu, Sigma, shape and rate) for the 
prior distribution.}

\item{offset}{an offset parameter}

\item{weights}{a weighting variable}

\item{family}{a description of the error distribution and link function to be used in the model. For \code{glmb} 
this can be a character string naming a family function, a family function or the result of a call to a family function. 
(See \code{\link{family}} for details of family functions.)}
}
\value{
Currently mainly the draws for the dispersion and the regression coefficients
will be updated to return outputs consistent with other function
}
\description{
Generates iid samples from the posterior density for the 
independent normal-gamma regression
}
\examples{

## ----dobson-------------------------------------------------------------------
## Dobson (1990) Page 93: Randomized Controlled Trial :
counts <- c(18,17,15,20,10,20,25,13,12)
outcome <- gl(3,1,9)
treatment <- gl(3,3)
print(d.AD <- data.frame(treatment, outcome, counts))

## ----Prior_and_Calls,results = "hide"-----------------------------------------
## Prior mean vector 
mu<-matrix(0,5)           
mu[1,1]=log(mean(counts)) 
## Prior standard deviation and Variance
mysd<-1           
V=((mysd)^2)*diag(5)  
prior=list(mu=mu,Sigma=V)
## Call to glmb
glmb.D93<-glmb(n=1000,counts ~ outcome + treatment,
               family = poisson(),prior=prior)


## ----glmb confint-------------------------------------------------------------
## confint for the glmb function
confint(glmb.D93)

}
\seealso{
Other simulation functions
\code{\link{rglmb_dispersion}()},
\code{\link{rglmb_norm_reg}()},
\code{\link{rindependent_norm_gamma_reg_temp_v2}()},
\code{\link{rindependent_norm_gamma_reg_temp}()},
\code{\link{rindependent_norm_gamma_reg_v1}()},
\code{\link{rindependent_norm_gamma_reg}()},
\code{\link{rnorm_gamma_reg}()}
}
\concept{simfuncs}
